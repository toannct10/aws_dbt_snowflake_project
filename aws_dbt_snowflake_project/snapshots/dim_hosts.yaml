snapshots:
  - name: dim_hosts
    relation: ref('hosts')
    config:
      database: AIRBNB
      schema: gold
      strategy: timestamp
      unique_key: HOST_ID
      updated_at: HOSTS_CREATED_AT
      dbt_valid_to_current: "to_date('9999-12-31')"